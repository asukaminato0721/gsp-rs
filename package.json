{
  "name": "gsp-rs-web-checks",
  "private": true,
  "packageManager": "pnpm@10",
  "scripts": {
    "generate:viewer-types": "cargo run --bin generate_viewer_types",
    "typecheck:js": "tsc -p tsconfig.json --noEmit",
    "typecheck:js:interactive": "tsc -p tsconfig.interaction.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.9.0"
  }
}
